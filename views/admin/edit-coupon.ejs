<!DOCTYPE html>
<html lang="en">
  <%- include('partials/_head.ejs') %>
<body class="g-sidenav-show  bg-gray-200">
  <%- include('partials/_navbar.ejs') %>
    <%- include('partials/_asidebar.ejs') %>
      <main class="main-content position-relative max-height-vh-100 h-100 border-radius-lg ">
        <div class="container-fluid py-4">
          <div class="row">
            <div class="col-12">
              <div class="card my-4">
                <div class="card-header p-0 position-relative mt-n4 mx-3 z-index-2">
                  <div class="bg-gradient-primary shadow-primary border-radius-lg pt-4 pb-3">
                    <h6 class="text-white text-capitalize ps-3">Edit Coupon</h6>
                  </div>
                </div>
                <div class="card-body px-0 pb-2">
                  <form id="jd-productForm" style="padding: 3rem" action="/admin/coupon/edit-coupon/<%= locals.coupon._id %>" method="POST">
                    
                    <label for="couponName">Name:</label><br>
                    <input type="text" id="couponName" name="couponName" value="<%= locals.coupon.name %>">
                    <label for="couponCode">Code:</label><br>
                    <input type="text" id="couponCode" name="couponCode" value="<%= locals.coupon.code %>">
                    <label for="discountType">Discount Type:</label><br>
                    <select id="discountType" name="discountType" value="<%= locals.coupon.discountType %>">
                      <option <%= locals.coupon.discountType === 'PERCENTAGE' && "selected" %> value="PERCENTAGE">PERCENTAGE</option>
                      <option <%= locals.coupon.discountType === 'FLAT' && "selected" %> value="FLAT">FLAT</option>
                      <!-- todo: Do the same kind of EJS-based drop down selection for product -->
                    </select>
                    <label for="discountValue">Discount Value:</label><br>
                    <input type="number" id="discountValue" name="discountValue" value="<%= locals.coupon.discountValue %>">
                    <label for="maxDiscount">Max Discount:</label><br>
                    <input type="number" id="maxDiscount" name="maxDiscount" value="<%= locals.coupon.maxDiscount %>">

                    <input type="submit" value="Edit">
                    <p style="color: red;font-weight:bold"><%= locals.error %></p>
                  </form>
                </div>
              </div>
            </div>
          </div>

        </div>
        <%- include('partials/_footer.ejs') %>
          </div>
      </main>
      <%- include('partials/_scripts.ejs') %>
</body>

</html>